## Real Time Hospital Data Web App

Basic real time hospital data web app using [Pusher](http://pusher.com/).

*Expects a file named 'config' in the root directory of the following format:*

     [PUSHER_CONFIG]
     KEY = <your Pusher key>
     SECRET = <your secret>
     APP_ID = <your app id>

